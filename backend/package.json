{
  "name": "@smsapi/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "check:fallback": "bun scripts/check-fallback.ts",
    "dev": "bun --env-file=../.env --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.7",
    "pino": "^10.3.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.3"
  }
}
